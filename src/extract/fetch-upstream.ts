/** Placeholder — CV1-03: fetch tarball / shallow clone from GitHub. */
export async function fetchUpstreamSnapshot(ref: string): Promise<string> {
  throw new Error(`Not implemented — see CV1-03 in dev-guide (ref: ${ref})`);
}
